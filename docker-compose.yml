version: '3.4'

services:
  wunderdb:
    image: wunderdb
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5002:5002
    volumes:
      - ${PWD}/secrets/server-config.json:/app/server-config.json
